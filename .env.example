# Dirección base de la API FastAPI. En producción DEBE ser https (RNF-003).
VITE_API_BASE_URL=http://localhost:8000
# true = la capa de datos resuelve contra src/api/mock; false = axios contra la API real.
VITE_USE_MOCK=true
